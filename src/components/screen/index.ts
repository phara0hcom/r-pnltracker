/**
 * Shared screen chrome: heading, stat tiles, section cards, meters, tables.
 *
 * One component per file, re-exported from here so every analysis screen still
 * pulls its chrome from a single import and reads as the same system.
 */
export { Empty } from './Empty'
export { Meter } from './Meter'
export { PageHeader } from './PageHeader'
export { Section } from './Section'
export { Stat } from './Stat'
export { StatGrid } from './StatGrid'
export { Table } from './Table'
